% Pell solutions
\newcommand{\sol}[1]{\ensuremath\mathcal P(#1)}
\newcommand{\solu}[1]{\ensuremath\units{\mathcal P}\!(#1)}

% best approximation and convergents
\newcommand{\Batest}[1]{\ensuremath \mathcal Q(#1)}
% \newcommand{\Bateste}[1]{\ensuremath #1[X] \times (\mino{#1[X]})}
\newcommand{\Bateste}[1]{\{(p,q) \in #1[X]^2 \mid q \neq 0 \}}
\newcommand{\Baset}[2]{\ensuremath \mathcal B_{#1}(#2)}
\newcommand{\Coset}[2]{\ensuremath \mathcal C_{#1}(#2)}

\newcommand{\CF}{\ensuremath\mathbf{CF}}  % continued fraction expansion
\newcommand{\QPL}{\ensuremath\mathcal{L}} % quasi period length
\newcommand{\PL}{\ensuremath\mathcal{L}}  % period length

% special matrices
\newcommand{\mcf}[1]{\mfour{#1}{1}{1}{}}
\newcommand{\mdi}[2]{\mfour{#1}{}{}{#2}}

% special rings
\newcommand{\Zai}{\Z[a_i \mid i \in \Z]}
\newcommand{\Zamn}{\Z[a_m, \dots, a_{m+n}]}

% sigma-reduced dual CF
\newcommand{\cfsb}[1]{\frac{-1\phantom{-}}{\sigma(#1)}}
